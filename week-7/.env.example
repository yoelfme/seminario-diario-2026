# Local Postgres from compose.yaml (host port 5433 → container 5432).
# The Prisma CLI scripts read DATABASE_URL from the environment; export it in your shell too:
#   export DATABASE_URL="postgresql://seminario:seminario@localhost:5433/seminario"

DATABASE_URL="postgresql://seminario:seminario@localhost:5433/seminario"
